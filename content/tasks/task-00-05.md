---
id: "00-05"
module: "00"
title: "Откатить, если Claude сломал"
difficulty: easy
estimated_minutes: 10
verified_date: null
---

## Цель

Научиться возвращать файлы к последнему коммиту, когда правка Claude оказалась неудачной.

## Инструкция (пошагово)

Это задача, ради которой был весь блок. Если после правки Claude проект перестал работать, а разбираться в причинах не хочется, самый быстрый путь — вернуть файлы к последней сохранённой точке и попросить заново, иначе.

1. Продолжайте в проекте из задачи 00-04. Убедитесь, что `git status` показывает `working tree clean`: последняя точка сохранена, есть куда откатываться.
2. Запустите `claude` и попросите нарочно грубую правку: «Перепиши README.md полностью: оставь только одну строку "Сломано"». Подтвердите.
3. Выйдите из Claude. `git diff` покажет много строк с `-` — весь прежний текст удалён. Представьте, что это была важная документация или рабочий код.
4. Верните всё как было:
   ```bash
   git restore .
   ```
   Точка в конце означает «все файлы в проекте». Команда ничего не выводит.
5. Проверьте: `git status` снова `working tree clean`, а в `README.md` прежний текст. Правка Claude исчезла бесследно, история коммитов не тронута.
6. Повторите шаги 2–4 через Claude: попросите любую правку, а затем скажите: «Откати все изменения к последнему коммиту». Claude выполнит `git restore` или похожую команду сам. Убедитесь по `git status`, что дерево чистое.

## Критерий выполнения

Задача выполнена, если после «ломающей» правки вы вернули файлы к последнему коммиту, и `git status` показывает `working tree clean`, а содержимое `README.md` прежнее.

## Подсказки

- `git restore .` отменяет изменения только в файлах, которые уже были в истории. Новые файлы, созданные Claude, останутся — они видны в `git status` как `Untracked`, их можно просто удалить.
- В модуле 3 встретится команда `git reset --hard HEAD` — она делает то же самое и чуть больше, поэтому там она сопровождается предупреждением. Для повседневного отката хватает `git restore .`.
- Откат необратим: то, что не было закоммичено, пропадает. Поэтому правило из прошлой задачи — коммит после каждого удачного шага — и есть то, что делает откат безопасным.
- Если правка Claude вам понравилась лишь частично, откатывать всё не обязательно: `git restore имя-файла` вернёт один файл, остальные останутся.
